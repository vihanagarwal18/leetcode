class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        // as there are more than 2 piles and sum of piles if odd 
        //so player who will pick first will always win
        //thus true in every case(alice wins)
        return true;
    }
};